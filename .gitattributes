# Normalise line endings so a Windows checkout (where Git for Windows defaults
# to core.autocrlf=true) does not rewrite every file to CRLF and leave the
# formatting hooks fighting git over each commit.
* text=auto eol=lf

# Binary / generated — never touch these.
*.png binary
*.jpg binary
*.ico binary
uv.lock -diff linguist-generated=true
